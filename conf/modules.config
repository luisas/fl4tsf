
process{

    withName: CENTRALIZED_TRAINING{
        publishDir = [
            path: { "${params.outdir}/centralized_training" },
            mode: "copy"
        ]
        if(params.use_gpu) {
            accelerator = 1 
            acceleratorType = 'gpu'
        }

    }
}